{
  "name": "subgraph-waterfall",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-waterfall",
    "deploy-hosted": "graph deploy --node https://api.thegraph.com/deploy/ rameez-dgc/TestGraph",
    "create-local": "graph create --node http://localhost:8020/ subgraph-waterfall",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-waterfall",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-waterfall"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "ethers": "^5.5.2"
  }
}
